{
  "name": "1zuxm-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute 1zuxm-db --local --file=schema.sql",
    "db:init:remote": "wrangler d1 execute 1zuxm-db --remote --file=schema.sql"
  },
  "dependencies": {
    "hono": "^4.7.4",
    "jose": "^6.0.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250204.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3",
    "wrangler": "^3.109.2"
  }
}
